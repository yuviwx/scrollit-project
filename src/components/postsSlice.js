import { createSlice } from "@reduxjs/toolkit/dist/createSlice";

export const postsSlice = createSlice({
    name: "posts",
    initialState: {
        posts: {}
    },
    reducers: {
        
    }
})